<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hash">	
	
	<select id="getHashListByGroup" resultType="hashvo" parameterType="memovo">
		<![CDATA[
			select a.no, a.g_no, a.m_no, a.name
			  from hash a, memo b
			 where a.m_no=b.no 
			   and b.u_no=#{uNo}
			   and(
				  (a.g_no is null and a.g_no is null)
				   or
				  (a.g_no is not null and a.g_no=#{gNo}));
 		]]>
	</select>
	
</mapper>
