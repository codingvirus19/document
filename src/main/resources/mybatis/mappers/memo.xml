<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="memo">

	<select id="personDeleteMemo" parameterType="memovo">
		<![CDATA[
			delete from memo where no=#{no} and g_no is #{gNo} and u_no=#{uNo};
 		]]>
	</select>
	<select id="memoHashDeleteMemo" parameterType="memovo">
      <![CDATA[
      delete
        from hash 
       where no in (select * 
                     from (select b.no 
                              from memo a, hash b 
                        where a.no = #{no}
                              and a.no = b.m_no
                             group by b.no) t);
       ]]>
   </select>
	<select id="peopleDeleteMemo" parameterType="memovo">
		<![CDATA[
			delete from memo where no=#{no} and g_no= #{gNo} and u_no=#{uNo};
 		]]>
	</select>

</mapper>
	