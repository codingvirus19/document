<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="alarm">

	<insert id="addAlarm" parameterType="alarmVo" useGeneratedKeys="true">
	 	<![CDATA[
			insert into alarm values(null, #{gNo}, #{chat}, now(), #{type})
		]]>
		<selectKey keyProperty="no" resultType="long" order="AFTER">
			select last_insert_id()
		</selectKey>
	</insert>

	<insert id="insertAccptAlarm" parameterType="java.util.HashMap">
	 	<![CDATA[
			insert into accptalarm(
				read_check,
				noti_no,
				u_no
			)values
		]]>
		<foreach collection="pushSandUserList" item="item"
			separator=",">
			(
			#{item.read_check},
			#{item.noti_no},
			#{item.u_no}
			)
		</foreach>
	</insert>

	<select id="getAlarmList" parameterType="alarmvo"
		resultType="alarmvo">
		<![CDATA[
		 select 
		 		max(a.read_check) as readCheck,
		 		max(b.type) as type
           from accptalarm a, (select no, g_no, massage, date, type from alarm) b
		  where a.noti_no = b.no
            and read_check = true
            and u_no=#{uNo}
            limit 10;
 		]]>
	</select>
	
</mapper>
	