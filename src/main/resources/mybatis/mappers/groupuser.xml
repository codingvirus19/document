<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="groupuser">

	<insert id="insert" parameterType="groupUservo">
	 	<![CDATA[
			insert 
			  into groupuser (u_no, g_no, a_no)
			 values (#{uNo}, #{gNo}, #{aNo});
		]]>
	</insert>

	<select id="getGroupinUser" parameterType="groupuservo" resultType="long">
		<![CDATA[
		 select u_no as uNo
		   from groupuser 
		  where g_no=#{gNo}
		    and u_no not in(#{uNo});
 		]]>
	</select>

	<insert id="alarmGroupJoin" parameterType="groupuservo">
	 	<![CDATA[
			insert 
			  into groupuser (u_no, g_no, a_no)
			 values (#{uNo}, #{gNo}, 3);
		]]>
	</insert>

</mapper>

