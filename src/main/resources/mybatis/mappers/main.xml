<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main">
	
	<select id="findAllMemo" parameterType="memovo" resultType="memovo">
		<![CDATA[
			select no, g_no as gNo, u_no as uNo, content, color, date, list_no as listNo
				from memo 
				where g_no= #{gNo}
				and u_no= #{uNo}
				order by list_no;
 		]]>
	</select>
	<select id="memoAtNull" parameterType="memovo" resultType="memovo">
		<![CDATA[
		 select no, u_no as uNo, content, color, date , list_no as listNo from memo where g_no is #{gNo} and u_no=#{uNo} order by  list_no;
 		]]>
	</select>
	
</mapper>
