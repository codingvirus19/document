<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main">
	
	<select id="findAllMemo" parameterType="groupvo" resultType="memovo">
		<![CDATA[
			select b.no, b.g_no as gNo, b.u_no as uNo, b.content as content, b.color as color, b.date as date
				from user a, memo b, groups c
				where a.no = b.u_no
				and b.g_no = c.no
				and c.no= #{no};
 		]]>
	</select>
	
</mapper>
