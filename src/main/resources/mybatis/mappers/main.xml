<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main">
	
	<select id="findAllMemo" parameterType="uservo" resultType="memovo">
		<![CDATA[
			select a.no as no, a.g_no as gNo, c.no as uNo, a.content as content, a.color as color, a.date as date
			from memo a, groups b, user c
			where a.g_no = b.no
			and b.u_no = c.no
			and c.id = #{id};
 		]]>
	</select>
	
</mapper>
